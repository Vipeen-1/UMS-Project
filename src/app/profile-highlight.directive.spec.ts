import { ProfileHighlightDirective } from './profile-highlight.directive';

describe('ProfileHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ProfileHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
